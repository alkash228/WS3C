# samv

Код WEB_samv разбит по папкам: http, api, анализ, видео, отчёты.

Запуск как раньше — `START_WEB_SAMV.bat` или `python serve.py`.

Зависимости: `pip install -r requirements.txt` (или `requirements-web.txt`).
Нужен **FFmpeg** в PATH.

Воркеры: `WEB_SAMV_WORKERS` (0 = сам подберёт).
